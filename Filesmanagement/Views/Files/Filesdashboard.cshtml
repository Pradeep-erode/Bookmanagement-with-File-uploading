@model IEnumerable<Bookmanagement.Core.FilesEntity.Filesentity>

@{
    ViewData["Title"] = "Filesdashboard";
    //var list = "~/Image/"+ Model;
}


<h1>Filesdashboard</h1>

<p>
    <a asp-action="Files">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                FileID
            </th>
            <th>
                Filename
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
       {
         <tr>
            <td>
                @Html.DisplayFor(modelItem => item.filesID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.filesname)
            </td>

            @*@if (item.filesview != null)
            {
                var base64 = Convert.ToBase64String(item.filesview);
                var Img = string.Format("data:Image/jpeg;base64,{0}", base64);
                <td><img src="@Img" class="border-0 img-fluid img-style" /></td>

            }*@

            <td><img src="~/Image/@item.filesname" alt="Image missing"/></td>
            
            <td>

                @*@Html.ActionLink("Download", "FileDownload", "Files", new { id = item.filesID })*@

                <a href="~/Image/@item.filesname">View</a>
                <a href="~/Image/@item.filesname" download>Download</a>

                @Html.ActionLink("Delete", "FileDelete", "Files", new { id = item.filesID })
            </td>
         </tr>
        }

    </tbody>

</table>